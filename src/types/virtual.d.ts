/** Built by the team-photos plugin in vite.config.ts: slug → public URL, for photos that exist. */
declare module 'virtual:team-photos' {
  const photos: Record<string, string>;
  export default photos;
}
