/** One quiet line about who runs the build. */
export function EngineLine() {
  return (
    <section className="container section engine-line" id="the-engine">
      <p className="t-body-lg dim">
        The build is run by an AI engine — agents and twins. This page is updated by it, not by
        us.
      </p>
    </section>
  );
}
